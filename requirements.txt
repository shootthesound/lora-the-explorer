# Only dependencies NOT in sd-scripts requirements.txt
gradio>=5.35.0
gradio_client>=1.10.4
torch
torchvision

# GUI-specific dependencies
pyyaml
tqdm

# Critical dependencies that might not install from sd-scripts
opencv-python
toml==0.10.2
imagesize==1.4.1

# LoRA-specific dependencies not in sd-scripts
lycoris-lora==1.8.3
omegaconf

# Install scipy before k-diffusion to ensure we get pre-built wheels on Windows
scipy

# k-diffusion and other dependencies
k-diffusion
peft