# Version constraints for musubi-tuner dependencies
# These override versions specified in musubi-tuner/requirements.txt

# Match musubi-tuner versions exactly for guaranteed compatibility
# PyTorch versions: cu128=2.8.0, cu124=2.7.1, cpu=2.8.0
accelerate==1.6.0
einops==0.7.0

# Match musubi-tuner versions for core ML libraries
diffusers==0.32.1
transformers==4.54.1
huggingface-hub>=0.34.3
safetensors>=0.4.5
sentencepiece>=0.2.1
